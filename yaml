config:
  remove_lines:
  - ^!.*$
  - ^$
global_keywords:
  aaa:
    accounting:
    - $type
    - $list_name
    - $event
    - remainder: methods
    authentication:
    - $type
    - $list-name
    - remainder: methods
    authorization:
      commands:
      - $priv
      - $list_name
      - remainder: methods
      exec:
      - $list_name
      - remainder: methods
    session-id:
  access-list:
    $number:
    - $action
    - $network
    - $netmask
  clock:
    timezone:
  control-plane:
  enable:
    password:
    - $encryption_type
    - $encrypted_password
    secret:
    - $encryption_type
    - $encrypted_password
  hostname:
    $name:
  interface:
    $name:
      context:
        bandwidth:
        channel-group:
        - $id
        - mode
        - $mode
        description:
        ip:
          address:
          - $ip
          - $mask
        shutdown:
        switchport:
          mode:
          - $modes
          trunk:
            allowed:
              vlan:
            encapsulation:
            native:
              vlan:
  ip:
    access-list:
      standard:
        $name:
          context:
          - $action
          - $ip
          - $mask
    domain-list:
    - $domains
    domain-name:
      $domain_name:
    http:
    name-server:
    - $name_server
    prefix-list:
      $name:
      - $sequence
      - $sequence
      - $action
      - $net_netmask
    route:
    - $network
    - $netmask
    - $nexthop
    routing:
    subnet-zero:
    tacacs:
      source-interface:
      - $interface
  line:
    $type:
    - $start
    - $finish
  logging:
    buffered:
      $level:
    console:
      $level:
    facility:
      $facility_type:
    monitor:
      $level:
  ntp:
    server:
      $ip:
    source:
      $interface:
  route-map:
  - $name
  - $action
  - $sequence
  - context:
      match:
      set:
  router:
    $protocol:
    - $process_id
    - context:
        distribute-list:
        - $name
        - $direction
        summary-address:
        - $network
        - $netmask
        passive-interface:
          $interface:
        router-id:
        log-adjacency-changes:
        auto-cost:
          reference-bandwidth:
            reference_bandwidth:
        redistribute:
        - $protocol
        - $process_id
        - remainder: qualifiers
        network:
        - $network
        - $mask
        - $area
        - $area
  service:
    pad:
    password-encryption:
    timestamps:
      $type:
      - $modifiers
  snmp-server:
    community:
    - $community_string
    contact:
    location:
    view:
    - $name
    - $oid
    - $action
  spanning-tree:
    mode:
      $mode:
  tacacs-server:
    directed-request:
    host:
    - $hostip
    key:
    - $encryption_type
    - $encrypted_password
    timeout:
      $value:
  udld:
  username:
  - $username
  - $password
  - $encryption
  - $password
  vlan:
